import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
//first parameter is the component we want to render and the next is the element we want to hook on to
ReactDOM.render(<App/>,document.getElementById('root'));


